"""LLM integration layer — model clients and response parsing.

This package will contain LLM client abstractions, retry logic, and
structured output parsing in Phase 2. The LLM layer is decoupled from
services and prompts to allow swapping providers without touching
business logic.
"""
